# streaming-yielding-app

Dotnet Core gRPC streaming example

Build and Run the gRPC server first

```powershell
cd .\Server
dotnet build
dotnet run
```

Then Build and Run the Client

```powershell
cd .\Client
dotnet build
dotnet run
```
